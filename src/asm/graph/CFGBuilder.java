package asm.graph;

import ir.LLVM;

public class CFGBuilder {
    public ControlFlowGraph build(LLVM llvm){
        ControlFlowGraph cfg = new ControlFlowGraph();
        return cfg;
    }
}
